这个服务算是单独的服务；然后不用写mapper层；而是使用Feign来调用item模块的Mapper接口
在Item模块连接数据库了；这里就不用了；所以Item模块要写对应的方法来给我上传服务调用，这样
把图片URL保存到数据库
(说错了；图片的URL不用保存到数据库的；直接保存到FastDFS服务器就行；所以不用使用Feign)

FastDFs必须要有一个配置类；不然启动报错；主要是bean重复的问题

测试类必须和启动类的目录结构一样；不然不行